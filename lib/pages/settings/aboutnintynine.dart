import 'package:flutter/material.dart';

class AboutNintyNite extends StatelessWidget {
  const AboutNintyNite({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('About Us'),
      ),
      body: Container(
        child: ListView(
          children: <Widget>[
            
          ],
        ),
      )
    
    );
  }
}